/**
 * @deprecated Removed in favour of the full-page edit form at
 * `/fleetspace/[slug]/edit`. This file is kept as an empty stub so any
 * stale imports surface as a clear "no exports" error rather than a
 * misleading "module not found" — it can be deleted entirely once the
 * change is reviewed.
 */
export {};
